const express = require("express");

const scoreRouter = express.Router();

scoreRouter.get('/',async (req,res)=>{
    try {
        
    } catch (error) {
        
    }
})



module.exports = {
    scoreRouter
}